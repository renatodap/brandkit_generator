# OpenRouter API (Required for SVG logo generation)
# Get your API key from: https://openrouter.ai/keys
# Used for: Multi-model SVG logo generation with Claude Sonnet 4 and Grok Code Fast
OPENROUTER_API_KEY=your_openrouter_api_key_here

# Groq API (Optional but recommended for AI-enhanced features)
# Get your API key from: https://console.groq.com/keys
# Used for: Intelligent symbol extraction, color mood analysis, font personality matching, design justifications
# Free tier: llama-3.1-8b-instant at $0.05/1M tokens
GROQ_API_KEY=your_groq_api_key_here_optional

# OpenAI API (Optional, for better tagline generation)
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here_optional

# App Configuration
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_APP_NAME="Brand Kit Generator"

# Sentry Error Tracking (optional but recommended for production)
# Get your DSN from: https://sentry.io/
NEXT_PUBLIC_SENTRY_DSN=your_sentry_dsn_here_optional
SENTRY_ORG=your_sentry_org
SENTRY_PROJECT=your_sentry_project

# Upstash Rate Limiting (optional but recommended for production)
# Get from: https://console.upstash.com/
UPSTASH_REDIS_REST_URL=your_upstash_redis_url_optional
UPSTASH_REDIS_REST_TOKEN=your_upstash_redis_token_optional

# Analytics (optional)
# NEXT_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# ============================================
# 🔐 SUPABASE AUTHENTICATION & DATABASE (Required)
# ============================================
# Get from: https://supabase.com/dashboard → Your Project → Settings → API
#
# Setup Steps:
# 1. Create a Supabase project at https://supabase.com
# 2. Go to Project Settings → API
# 3. Copy the Project URL, anon key, and service_role key
# 4. Run the SQL schema: supabase-schema.sql in SQL Editor
# 5. Enable Email auth in Authentication → Providers
#
# SECURITY WARNING:
# - NEXT_PUBLIC_SUPABASE_ANON_KEY: ✅ Safe for client-side (respects RLS)
# - SUPABASE_SERVICE_KEY: ❌ NEVER expose in client code (bypasses RLS)
# - SUPABASE_ACCESS_TOKEN: Only for Supabase CLI/migrations

# Project URL (safe to expose)
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co

# Anon/Public Key (safe for client-side, respects Row Level Security)
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGc...your_anon_key_here

# Service Role Key (SERVER-ONLY, bypasses ALL security policies)
# ⚠️ CRITICAL: Only use in API routes, never in client components
SUPABASE_SERVICE_KEY=eyJhbGc...your_service_role_key_here

# Access Token (optional, for Supabase CLI and database migrations)
# Get from: https://supabase.com/dashboard/account/tokens
SUPABASE_ACCESS_TOKEN=sbp_your_access_token_here_optional
