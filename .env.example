# ============================================================================
# Brand Kit Generator - Environment Variables
# ============================================================================
# Copy this file to .env.local and fill in your actual values
# NEVER commit .env.local to version control!

# ============================================================================
# Supabase Configuration (REQUIRED)
# ============================================================================
# Get these from: https://supabase.com/dashboard/project/_/settings/api

# Client-safe Supabase keys (safe to use in browser)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Server-only Supabase keys (NEVER expose in client code!)
SUPABASE_SERVICE_KEY=your-service-role-key-here
SUPABASE_ACCESS_TOKEN=your-access-token-here-for-cli-optional

# ============================================================================
# AI API Keys
# ============================================================================

# OpenRouter (REQUIRED for SVG logo generation)
# Get from: https://openrouter.ai/keys
OPENROUTER_API_KEY=your-openrouter-api-key-here

# Groq (Optional - for fast, cost-effective LLM calls)
# Get from: https://console.groq.com/keys
GROQ_API_KEY=your-groq-api-key-here-optional

# OpenAI (Optional - for advanced features)
# Get from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your-openai-api-key-here-optional

# Hugging Face (Optional - for image generation fallback)
# Get from: https://huggingface.co/settings/tokens
HUGGINGFACE_API_KEY=your-huggingface-api-key-here-optional

# ============================================================================
# Application Configuration
# ============================================================================

# App URL (used for redirects and email links)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# App Name (displayed in UI)
NEXT_PUBLIC_APP_NAME=Brand Kit Generator

# Node Environment
NODE_ENV=development

# ============================================================================
# Optional Services
# ============================================================================

# Upstash Redis (Optional - for rate limiting)
# Get from: https://console.upstash.com/
UPSTASH_REDIS_REST_URL=https://your-redis-url.upstash.io
UPSTASH_REDIS_REST_TOKEN=your-redis-token-here

# Sentry (Optional - for error tracking in production)
# Get from: https://sentry.io/settings/projects/your-project/keys/
SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id
SENTRY_ORG=your-org-name
SENTRY_PROJECT=your-project-name
NEXT_PUBLIC_SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id

# ============================================================================
# Setup Instructions
# ============================================================================
#
# 1. Create a Supabase project at https://supabase.com
# 2. Copy the URL and anon key from Settings > API
# 3. Copy the service role key (CAUTION: Never expose this in client code!)
# 4. Run the database migrations in supabase/migrations/
# 5. Get an OpenRouter API key from https://openrouter.ai
# 6. (Optional) Get Groq API key for faster, cheaper AI calls
# 7. Copy this file to .env.local and fill in your values
# 8. Run: npm install
# 9. Run: npm run dev
#
# For production deployment:
# - Set all environment variables in your hosting provider (Vercel, Railway, etc.)
# - Ensure NODE_ENV=production
# - Update NEXT_PUBLIC_APP_URL to your production domain
# - Enable Sentry for error tracking (recommended)
# - Configure Upstash Redis for rate limiting (recommended)
